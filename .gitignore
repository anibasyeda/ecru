# Dependencies (installed per-machine with `npm install` — never committed)
node_modules/

# Environment files / secrets
.env
.env.local
.env.*.local

# Build output
dist/
build/

# Logs
*.log
npm-debug.log*

# Personal deployment notes — keep local, not on GitHub
DEPLOYMENT.md

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
